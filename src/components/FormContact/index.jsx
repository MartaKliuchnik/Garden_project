import React from 'react';
import s from './style.module.sass';

export default function FormContact() {
    return (
    <div className={s.wrapper}>
        <div className={s.subheader}>
            <p className={s.title_subheader}>Contact form</p>
            <p>Do you have a question, comment or request? Let us know by using the contact form below and we will respond as soon as possible.</p>
        </div>
        <form>
            
        </form>
    </div>
    )
}
